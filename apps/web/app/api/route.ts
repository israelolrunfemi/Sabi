export { proxyRequest as GET, proxyRequest as POST, proxyRequest as PUT, proxyRequest as PATCH, proxyRequest as DELETE, proxyRequest as OPTIONS, proxyRequest as HEAD } from "@/lib/api/proxy";

export const dynamic = "force-dynamic";
export const revalidate = 0;
